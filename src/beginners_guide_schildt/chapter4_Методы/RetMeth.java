package beginners_guide_schildt.chapter4_Методы;

/*class RetMeth {
    public static void main(String args[]) {
        Vehicle minivan = new Vehicle();
        Vehicle sportscar = new Vehicle();

        //int range1, range2;

        // assign values to fields in minivan
        minivan.passengers = 7;
        minivan.fuelcap = 16;
        minivan.mpg = 21;

        // assign values to fields in sportscar
        sportscar.passengers = 2;
        sportscar.fuelcap = 14;
        sportscar.mpg = 12;

        // get the ranges
        //range1 = minivan.range();//Присвоение переменной значения, возвращаемого методом
        //range2 = sportscar.range();


        //System.out.println("Minivan can carry " + minivan.passengers +
        //        " passengers with range of " + range1 + " miles");
        System.out.println("Minivan can carry " + minivan.passengers +
                " passengers with range of " + minivan.range() + " miles");

        //System.out.println("Sportscar can carry " + sportscar.passengers +
        //        " passengers with range of " + range2 + " miles");
        System.out.println("Sportscar can carry " + sportscar.passengers +
                " passengers with range of " + sportscar.range() + " miles");
        if(minivan.range() > sportscar.range()) System.out.println("minivan can carry more than sportscar");
    }
}*/

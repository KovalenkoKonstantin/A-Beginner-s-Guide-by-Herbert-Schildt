package chapter14_Lambda.tasks;
/**Предположим, имеется класс MyClass, содержащий объектный метод
 myinstMethod(), и относящийся к этому классу объект mcObj. Продемонстрируйте,
 каким образом можно создать ссылку на метод myinstMethod(), ассоциированный
 с объектом mcObj.*/
public class MyClassv1 {
    MyClassv1 mcObj;

    public boolean myinstMethod(int i) {
        return false;
    }
}
